---
import { pickColorSchemeByStringHash } from "@/util";
import Tag from "./Tag.astro";

const {
  project: { icon, title, description, url, techStack },
} = Astro.props;
---

<div
  class="backdrop-blur bg-rose-500 bg-opacity-5 shadow-2xl outline outline-1 outline-gray-500 transition-all hover:-translate-y-3 flex flex-col sm:flex-row max-w-sm rounded-lg overflow-hidden p-8 space-y-4 md:space-y-0 md:space-x-4 items-start"
>
  <div>
    <img src={icon} alt={title} width="96" height="96" />
  </div>
  <div class="flex flex-col">
    <div class="text-2xl font-bold leading-none">{title}</div>
    <div class="mt-2">{description}</div>
    <!-- <div class="w-full">
      {
        techStack.map((tech) => (
          <Tag color={pickColorSchemeByStringHash(tech)} text={tech} />
        ))
      }
    </div> -->
    <!-- <Link
      as={Button}
      href={url}
      isExternal
      variant="outline"
      size="lg"
      colorScheme="blue"
      alignSelf="end"
    >
      View Project
    </Link> -->
  </div>
</div>
